{
  "name": "@odanado/config-loader",
  "version": "0.0.2",
  "license": "MIT",
  "main": "dist/index.js",
  "module": "lib/index.js",
  "types": "dist/index.d.ts",
  "files": [
    "dist",
    "lib"
  ],
  "dependencies": {},
  "devDependencies": {
    "@types/jest": "^24.0.15",
    "@types/node": "^12.6.2",
    "coveralls": "^3.0.5",
    "jest": "^24.8.0",
    "ts-jest": "^24.0.2",
    "typescript": "^3.5.3"
  },
  "scripts": {
    "build": "yarn build:cjs && yarn build:esm",
    "build:cjs": "yarn tsc -p tsconfig.build.json",
    "build:esm": "yarn tsc -p tsconfig.module.json",
    "test:build": "tsc --noEmit",
    "test:jest": "jest",
    "report:coverage": "cat coverage/lcov.info | yarn coveralls"
  },
  "publishConfig": {
    "access": "public"
  }
}
